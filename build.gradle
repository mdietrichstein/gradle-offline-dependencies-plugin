apply plugin: 'groovy'

archivesBaseName = 'offline-dependencies-plugin'
group = 'io.pry.gradle'
version = '1.0-SNAPSHOT'

repositories {
  mavenCentral()
}

dependencies {
  compile gradleApi()
  compile localGroovy()
}
